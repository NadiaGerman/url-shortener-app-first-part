# Placeholder: Lambda function code will go here
# See the main README and lab steps for full code after we create the DynamoDB table and finalize endpoints.

def handler(event, context):
    return {
        "statusCode": 200,
        "body": "Lambda deployed successfully. Implement logic next."
    }
